# content.css is generated by sass, which always writes LF. Without this the
# file comes back modified after every build on Windows even though not one
# character of CSS has changed, which invites a meaningless diff into a
# commit and hides a real one.
code/content.css text eol=lf
